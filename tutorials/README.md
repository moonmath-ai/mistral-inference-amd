# Tutorials
Original notebook tutorials provided in the Minstral repo are not tracked in this repo, but their jupytext scripts are.
To regenerate the notebooks run `jupytext --sync **/*.py` in this directory.

Alternatively, to store changes in notebook inputs, run `jupytext --sync **/*.ipynb`.
